extends layout

block content
  h1= title
  if show
    form(method='post')
      p Jaka jest Twoja ulubiona pora roku?
      ul
        each item in data
          li
            label=item.title
            input(type='radio' name='quiz' value=item._id)
        input(type='submit' value='Zagłosuj')
        p Aby zobaczyć wyniki oddaj głos
  else
    h2 Dokonałeś już wyboru w tej ankiecie
    p Wyniki
      ul
        each item in data
          li #{item.title} głosów: #{item.votes}(#{Math.round(item.votes / sum * 100)}%)
      p Głosów oddano: #{sum}